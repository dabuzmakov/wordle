# Wordle

Консольная игра в духе Wordle на C#. 
Программа загадывает слово из пяти русских букв, у игрока шесть попыток. После каждой попытки показывается результат для каждой буквы

## Текущее состояние

Реализован первый этап проекта (MR1):

- словарь из 50 русских слов;
- проверка длины слов;
- выбор загаданного слова по `seed`;
- одинаковый `seed` даёт одинаковое слово;
- пустой словарь отклоняется;
- модель игровой сессии;
- ограничение количества попыток;
- состояния игры: `InProgress`, `Win`, `Lose`;
- история попыток;
- тесты обязательных требований MR1.

## Структура проекта

```text
Wordle/
├── Wordle.App/
│   ├── GameSession.cs
│   ├── GameStatus.cs
│   ├── WordDictionary.cs
│   ├── WordSelector.cs
│   └── Program.cs
│
└── Wordle.Tests/
    └── Mandatory/
        └── Mr1/
            ├── DictionaryTest.cs
            └── SeedTest.cs